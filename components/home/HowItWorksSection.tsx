import { ProcessSection } from './ProcessSection';

/**
 * Home-page section explaining the lifecycle.
 * Kept as a stable export because other pages (and older commits) may import it.
 */
export function HowItWorksSection() {
  return <ProcessSection />;
}
